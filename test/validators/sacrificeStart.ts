//const mocks = require('../mocks/').sacrificeStart;
//const {sacrificeStart} = require('../../src/validators');
//const _ = require('lodash');
//const { expect, assert } = require('chai');
//const deepfreeze = require('deep-freeze');

export default function sacrificeStartSpec(): void {
  it("cannot start a sacrifice after one has already started");
  it("cannot start a sacrifice after a basic action has been taken");
}
